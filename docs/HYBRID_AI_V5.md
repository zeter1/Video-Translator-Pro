# Hybrid AI v5

New components:
- model installer/status planner;
- user glossary;
- translation context buffer;
- TTS health tracking.

Pipeline:

Whisper -> Local Translation -> Quality Check -> Google Repair -> TTS Router

Large models are never downloaded silently.
