# Hybrid Translation v3

Architecture:

Whisper -> Local engines (Argos/Marian) -> Quality check -> Google repair only for bad segments.

TTS fallback:
Edge Neural -> Piper offline -> Windows voice.

Models show download and installed sizes.
