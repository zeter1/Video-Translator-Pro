# Legacy artifacts

`old_bat/` contains older launch/install BAT variants that were present under encoded filenames in the source archive. Active Windows entrypoints are the root `Запустить.bat` and `Установить_зависимости.bat`.
